<Window x:Class="NBank.Merge.Merge"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NBank.Merge"
        mc:Ignorable="d"
        Title="Merge" Height="auto" Width="500"
        
        x:Name="frmMerge"
        Loaded="frmMerge_Loaded" WindowStartupLocation="CenterScreen"
        >
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="Gray" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="Red"/>
        </Style>
    </Window.Resources>
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="5"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="5"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="5"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="5"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="5"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="5"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"></ColumnDefinition>
            <ColumnDefinition Width="5"></ColumnDefinition>
            <ColumnDefinition Width="50*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock  Grid.Row="0" Grid.ColumnSpan="3" TextWrapping="WrapWithOverflow" x:Name="lblWarningText"  >
            This is Critical Data Operation
<LineBreak/>
Ensure there is no other user presently logged in


        </TextBlock>
        <Label Grid.Row="2" Grid.Column="2" Content="To Company"></Label>
        <Label Grid.Row="2" Grid.Column="0" Content="From Company"></Label>

        <ComboBox Grid.Row="4" Grid.Column="2" x:Name="cmbToCompanyList" SelectionChanged="cmbToCompanyList_SelectionChanged" ></ComboBox>
        <ComboBox Grid.Row="4" Grid.Column="0" x:Name="cmbFromCompanyList" SelectionChanged="cmbFromCompanyList_SelectionChanged"></ComboBox>

        <ComboBox Grid.Row="6" Grid.Column="2" x:Name="cmbToCompanyProjectList"  SelectionChanged="cmbToCompanyProjectList_SelectionChanged" ></ComboBox>
        <ComboBox Grid.Row="6" Grid.Column="0" x:Name="cmbFromCompanyProjectList" SelectionChanged="cmbFromCompanyProjectList_SelectionChanged" ></ComboBox>

      

        <StackPanel Grid.Row="8" Grid.ColumnSpan="3" Width="100" HorizontalAlignment="Right" >
            <Button Content="_Merge" Padding="5,5,5,5" Width="100" x:Name="btnMerge" Click="btnMerge_Click" ></Button>
        </StackPanel>

    </Grid>
</Window>
